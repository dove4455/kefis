import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WprocessedComponent } from './wprocessed.component';

describe('WprocessedComponent', () => {
  let component: WprocessedComponent;
  let fixture: ComponentFixture<WprocessedComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WprocessedComponent ]
    })
    .compileComponents();
  }));

});
